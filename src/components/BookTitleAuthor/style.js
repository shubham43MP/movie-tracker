import { makeStyles } from '@material-ui/core/styles'

export default makeStyles((theme) => ({
  titleTypography: {
    width: 'inherit',
    marginTop: '2%',
    fontWeight: 600
  },
  authorTypography: {
    fontWeight: 600
  }
}))
